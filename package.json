{
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  },
  "dependencies": {
    "netlify-cli": "^15.0.2"
  },
  "scripts": {
    "build":"npx @tailwindcss/cli -i css/style.dev.css -o style.public.css",
    "buildWatch":"npx @tailwindcss/cli -i css/style.dev.css -o style.public.css --watch"
  }
}
